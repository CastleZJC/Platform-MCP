# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Test / coverage
.coverage
htmlcov/
.pytest_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# CodeGraph
.codegraph/

# Claude Code local settings
.claude/

# PostgreSQL data (remote DB only, no local)
PostgreSQL/

# Logs
logs/
*.log

# Node（node_modules 任意层级；根锚定 package-lock 仅排除根目录误生成物，前端 lock 文件已跟踪不受影响）
node_modules/
/package-lock.json

# Environment / secrets
*.key
.env
settings-prod.yml
cookies.txt
coverage.json

# OS
Thumbs.db
.DS_Store
poc/config.yml
platform-mcp-frontend/coverage/
settings.yml

# Internal doc, not for publish
环境切换.md

# Internal deployment artifacts (specific to internal production env, not for publish)
documents/aduit/
documents/tst/
poc/
remote/
documents/design/*开发计划*.md

# Deployment helper scripts (hardcoded with internal production hosts)
scripts/_verify_db_mcp.py
scripts/_verify_149.py
scripts/_test_mcp_auth.py
scripts/_import_poc_datasources.py
scripts/_ssh_helper.py
scripts/_sftp_upload_all.py
scripts/_upload_wheels.py
scripts/_upload_one.py
scripts/_upload_build_tools.py
scripts/_build_manual.py
scripts/_sanitize_residual.py

# Binary office documents (internal-only; use .md for publishable docs)
*.docx
*.doc
*.pdf
*.xlsx
*.xls
*.pptx

# Internal DB archive (historical drafts, not for publish)
documents/db/历史存档/

# Migration data backups (local ops safety net, not for publish)
documents/db/backup/

# Internal design archive (historical drafts, not for publish)
documents/design/历史存档/

# Internal milestone review checklists (not for publish)
documents/review/

# Internal skill reference files (not for publish)
documents/skill/

# SFTP transfer exchange dir (runtime artifact, BUG20260814163941)
sftp_exchange/
# Skill 上传/草稿存档运行时目录（启动自动生成）
uploads/
